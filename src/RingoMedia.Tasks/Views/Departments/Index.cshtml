@model IEnumerable<RingoMedia.Tasks.Domain.DbEntities.Department>

@{
    ViewData["Title"] = "Departments";
}

<h1>Departments</h1>

<a class="btn btn-primary" asp-action="Create">create departemnt</a>
<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var department in Model)
        {
            <tr>
                <td>@department.Name</td>
                <td>
                    <a asp-action="Details" asp-route-id="@department.Id">View</a>
                </td>
            </tr>
        }
    </tbody>
</table>
